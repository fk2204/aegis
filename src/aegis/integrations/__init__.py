"""Third-party integrations that AEGIS pulls FROM (not Bedrock / Supabase /
Close — those have their own first-class modules).

Currently empty — the funder guidelines sync now reads from a local
folder on the operator's Windows workstation (see
``scripts/sync_funders_from_folder.py``) instead of calling out to any
third-party API. Re-populated when a new outbound integration ships.
"""
