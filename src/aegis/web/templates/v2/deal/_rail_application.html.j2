{# _rail_application.html.j2 — application KV rows from the Close FINANCIAL block. #}
<section class="card">
  <div class="head"><h2>Application</h2></div>
  <div class="bd">
    {% for row in application.rows %}
    <div class="kv"><span class="k">{{ row.k }}</span><span class="v">{{ row.v }}</span></div>
    {% endfor %}
  </div>
</section>
