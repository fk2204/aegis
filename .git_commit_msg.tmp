feat(compliance): override flywheel — auto-link overrides to deal outcomes

src/aegis/compliance/override_outcome_links.py — junction repository
(Pydantic OverrideOutcomeLink + Protocol + InMemory + Supabase
backends) connecting operator overrides to recorded deal outcomes.
Dependency-injection wiring in api/deps.py.

Route-side auto-link into /ui/overrides/summary follows in a
separate commit when migration 098 lands. One template-integration
test is skipped pending that.

Migration 098 (creates override_outcome_links junction) is held
locally — NOT in this commit.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
